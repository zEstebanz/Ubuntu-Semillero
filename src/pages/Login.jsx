import { Box } from "@mui/material"
import { GoogleButton } from "../components/Login/GoogleButton"

export const Login = () => {

    return (
        <Box
            sx={{
                
            }}
        >
            <GoogleButton />
        </Box>
    )
}